import React from 'react';

import Header from '../Header';
import Projects from '../Projects';

const Home = props => {
  return (
    <React.Fragment>
      <Header />
      <Projects />
    </React.Fragment>
  );
};

export default Home;
